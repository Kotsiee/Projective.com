/**
 * Package-level re-export of the roving active-index hook for listbox/menu keyboard models
 * (canonical implementation with the fields taxonomy).
 */
export { useListNavigation } from "../fields/hooks/useListNavigation.ts";
export type { ListNavigation } from "../fields/hooks/useListNavigation.ts";
