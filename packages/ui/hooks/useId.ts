/**
 * Package-level re-export of the SSR-safe id hook (canonical implementation with the fields
 * taxonomy). Gives every taxonomy one stable `@projective/ui/hooks` import path.
 */
export { useId } from "../fields/hooks/useId.ts";
