/**
 * colors — re-exported from `@projective/types/files`, where the weighting now lives so the server-side
 * media pipeline and this browser extractor summarise a picture identically. Kept at this path so
 * existing imports (and `colors.test.ts`) are unchanged.
 */
export { averageColor, colorSummary, dominantColors } from "@projective/types/files";
