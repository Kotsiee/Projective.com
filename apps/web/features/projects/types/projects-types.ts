/**
 * Projects domain types — sourced from the Zod SSOT at `@projective/types/projects`.
 *
 * App components/islands import shapes through this shim (the stable in-feature path); the fat
 * `@projective/backend` service imports the package directly. Mirrors `explore/types/explore-types.ts`.
 */
export * from "@projective/types/projects";
